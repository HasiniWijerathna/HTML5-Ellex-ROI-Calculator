import { describe, expect, it } from '@angular/core/testing';

describe('roiValidator', () => {

  it('should calculate', () => {
    expect((2 + 2) * 5 * 5).toBe(100);
    expect((4 + 4) * 10 * 10).toBe(800);
  });

});
